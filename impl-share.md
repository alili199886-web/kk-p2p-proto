# 金刚体育 P2P 挂单分享 实现文档

> 基于 2026-07-31 需求定稿（《P2P 挂单分享功能需求说明文档 V1.1》）与交互原型 v5.2 整理，用于体育与金刚 / IM 侧同步实现方案。涉及：
> - 体育 H5：`Vue 3 + TypeScript + Vite`
> - 金刚 App / IM SDK：`im2-link-jssdk`
>
> 体育侧无原生 App，只有 H5。H5 会运行在三类容器中：金刚 App WebView、现金盘站点内嵌、手机浏览器。前两类容器由宿主注入了 IM SDK，手机浏览器没有，因此分享能力按容器分级，详见 2.0。

---

## 一、总览

体育的"分享"**没有邀请码、没有二维码、没有落地推广页、没有佣金返利**。
分享 = 把**一笔 P2P 挂单**分享给好友，好友点开后直进该投注项并弹出购物车。**庄单 ID 就是唯一的分享凭据**。
分享方式为**金刚分享**（webview 消息体）与 **Telegram 链接分享**。

整条链路跨三层：

```
┌─────────────────┐   IM.share    ┌──────────────┐   拉取内容    ┌─────────────────┐
│  体育 H5         │ ────────────► │ 金刚 App /IM │ ───────────► │ 体育 分享内容页  │
│  卖单/卖出详情    │  分享链接      │ 组装消息体    │  每次实时请求 │ 公开只读 免登录  │
└─────────────────┘               └──────────────┘              └─────────────────┘
                                         │ 好友点击 webview
┌─────────────────┐   URL 参数           ▼
│  体育 H5         │ ◄───────────── 唤起体育 App
│  校验状态→落地   │   订单ID              
└─────────────────┘
```

**职责划分：**

| 层 | 做什么 | 不做什么 |
|---|---|---|
| 体育 H5 | 判定运行容器并决定分享按钮展示；生成含庄单 ID 与商户标识的公开链接；金刚容器内调 IM SDK 发起分享；提供公开内容页并返回实时数据；回流时解析参数、按商户路由、校验状态、落地投注 | 不组装 IM 卡片样式、不做注册 |
| 现金盘 | 提供体育入口的可唤起地址，供落地时按商户标识路由 | 不参与分享内容组装 |
| 金刚 App / IM | 按链接渲染 webview 消息体；每次渲染/刷新实时拉取内容；点击后唤起体育并透传订单 ID | 不缓存内容快照、不解析业务状态 |
| Telegram | 按链接抓取 OG 元信息生成会话预览 | — |

---

## 二、分享发起（体育 H5 → IM SDK）

### 2.0 运行容器与能力分级

| 容器 | IM SDK | 分享到金刚 | 分享到 TG | 复制链接 | 商户归属 |
|---|---|---|---|---|---|
| 金刚 App WebView | 有 | 展示，调 `IM.share` | 展示 | 展示 | 金刚体育默认商户 |
| 现金盘站点内嵌 | 有 | 展示，调 `IM.share` | 展示 | 展示 | 该现金盘商户 |
| 手机浏览器 | 无 | **不展示** | 展示 | 展示 | 按进入来源判定 |

**按钮展示判定规则：**

- 「分享到金刚」以**容器判定**代替"是否已安装"：仅当宿主注入了 IM SDK（金刚 App 容器与现金盘内嵌容器均已注入）时展示。此时用户必然已安装金刚通讯，不存在唤起失败；未注入 SDK 的容器（手机浏览器）不展示该按钮；
- 「分享到 TG」常驻展示。TG 分享走网页链接 `https://t.me/share/url?url={分享链接}&text={标题}`，未安装 TG 时浏览器会打开 TG 网页版并由 TG 自带下载引导，不会出现白屏或无响应；
- 「复制链接」在所有容器常驻，是最终兜底；
- 某容器下两个分享按钮都不展示时，分享层提示「当前环境不支持直接分享，请复制链接后手动发送」。

> **技术约束**：浏览器不提供查询"某 App 是否已安装"的接口，无法在点击前预判安装状态。因此「未安装则不展示按钮」通过上述容器判定实现；对 TG 采用"网页链接兜底"而非隐藏按钮，因为该方式在未安装时依然能完成分享。

**唤起失败降级**（用于后续可能新增的 scheme 唤起场景）：点击后尝试唤起并启动 2 秒计时，若期间 `visibilitychange` 未触发（页面未被切走），判定唤起失败，弹出降级弹窗，提供「复制链接」「下载对应应用」「取消」三个操作。

### 2.1 分享入口（2 类）

入口仅在挂单**可被购买**时出现，失效挂单不展示分享按钮。

| 位置 | 展示条件 | 说明 |
|---|---|---|
| 卖单列表卡片右上角分享图标 | 挂单状态 = 卖出中 / 部分成交 | 点击不触发卡片跳转，需阻止事件冒泡 |
| 庄家卖出详情页顶栏右上角分享图标 | 同上 | 已售罄 / 已封盘 / 已关闭 / 已结算时隐藏 |

点击后弹出分享层，包含：内容预览卡（与接收方所见一致）、分享链接文本行（附"复制"）、"分享到金刚"、"分享到 Telegram"、"取消"。

### 2.2 分享链接结构

链接由体育生成，为**完整可访问 URL**，公开免登录，长期有效：

```
https://{p2p_share_host}/p2p/s/{庄单ID}?m={商户标识}

示例（金刚体育）：https://sports.kk.com/p2p/s/S20260731001?m=kksport
示例（现金盘）：  https://sports.kk.com/p2p/s/S20260731001?m=cashbet01
```

- `m` 为**商户标识**，取分享发起时体育 H5 所在容器的商户，用于决定接收方点击后回到哪个平台的体育入口（见 4.2）；
- `m` 缺失或非法时按金刚体育默认商户处理。

- `{p2p_share_host}` 取自全局配置，便于环境切换与域名更换；
- `{庄单ID}` 为该投注项庄单唯一标识，一个链接对应一笔挂单；
- 同一挂单多次分享复用同一链接，不做二次生成；
- 挂单失效后链接**仍可打开**，展示失效态与引导，不返回 404。

### 2.3 调用 IM SDK 分享（金刚分享）

SDK 使用 `im2-link-jssdk`，`IMSDK_ID` / `IMSDK_TOKEN` 取自环境变量。体育侧调用形态：

```ts
export function useSportsShare(shareUrl: string, orderId: string, title: string) {
  if (!IM?.share) return
  IM.share({
    param: {
      show_type: 2,          // 带链接样式；webview 承载方式待金刚确认（见第六章）
      game_path: shareUrl,   // 体育生成的完整链接
      subGameName: title,    // 分享卡片标题（多语言，见 2.4）
    },
    type: 'cardGame'         // 若金刚提供 webview 专用类型，替换为该类型
  })
}
```

> 体育传给 SDK 的是**完整可访问 URL**（内容页归体育所有），不是需要宿主二次拼接域名的 query 片段。

### 2.4 分享参数说明

| 参数 | 类型 | 必填 | 体育侧取值 |
|---|---|---|---|
| `show_type` | `0｜1｜2` | 是 | 固定 `2`（带链接样式） |
| `game_path` | `string` | 是 | 完整分享 URL（见 2.2） |
| `subGameName` | 多语言对象 | 否 | 分享标题，字段 `ch/en/zh/th/vi`，内容为「赛事 + 投注项 + 赔率」摘要 |
| `currencySource` | `string` | 否 | 体育不传（分享内容不含币种资金信息） |

### 2.5 埋点

| 事件 | 触发时机 | 附带参数 |
|---|---|---|
| `p2p_share_sheet_show` | 分享层曝光 | 庄单 ID、当前状态 |
| `p2p_share_copy` | 点击复制链接 | 庄单 ID |
| `p2p_share_kk` / `p2p_share_tg` | 点击分享到金刚 / TG | 庄单 ID |
| `p2p_share_page_view` | 分享内容页加载 | 庄单 ID、当次状态 |
| `p2p_share_cta_click` | 主按钮点击 | 庄单 ID、落地结果（有效 / 失效） |
| 成交来源标记 | 落地后产生成交 | 注单标记来源 = 分享落地 |

本期仅上报，不建设后台分析页面；不做渠道归因与拉新奖励。

---

## 三、分享内容页（体育提供，金刚 webview 渲染）

### 3.1 路由与访问控制

- 路由：`/p2p/s/:orderId`，**公开可访问，无需登录、无需授权**；
- 不携带、不返回任何用户身份信息；
- 不提供投注能力，投注一律回到体育 App 内完成；
- 功能开关 `p2p_share_enabled = 0` 时，页面展示「功能暂不可用」，不返回错误页。

### 3.2 页面字段

| 区域 | 字段 | 规则 |
|---|---|---|
| 品牌区 | 产品 LOGO 与名称 | 固定展示 KING KONG 体育标识与「P2P」标记 |
| 赛事区 | 联赛、开赛时间、主队 VS 客队 | 取实时赛事信息 |
| 挂单区 | 玩法 | 如「全场 让球」「全场 独赢」 |
| 挂单区 | 投注项 | 如「主 +0.5/1」「大 3」，加粗突出 |
| 挂单区 | 卖出赔率 | 港盘，橙色加粗，取实时值 |
| 挂单区 | 庄家 | 挂单所属庄家标识 |
| 状态区 | 挂单状态 | 卖出中 / 部分成交 / 已售罄 / 已封盘 / 已关闭 / 已结算 |
| 状态区 | 失效说明 | 非可购买态时展示原因文案 |
| 底部 | 信息更新时间 | 展示本次获取时间，并说明每次打开自动获取最新信息 |
| 底部 | 主按钮 | 有效：「立即前往投注」；失效：「前往查看其他挂单」 |

**不展示**：剩余可卖额度、押金、总额度等庄家资金信息（避免暴露资金体量）。

### 3.3 实时性要求

- 每次页面加载或刷新均请求最新数据，**不使用缓存快照**；
- 金刚侧每次渲染 / 刷新 webview 均需重新拉取，不得复用本地缓存（见第六章对接项）；
- 网络异常时展示占位与重试入口，**不展示历史缓存数据**，避免过期信息误导。

### 3.4 状态与主按钮映射

| 挂单状态 | 内容页展示 | 主按钮 |
|---|---|---|
| 卖出中 / 部分成交 | 状态标签 + 完整挂单信息 | 立即前往投注 |
| 已售罄 | 已售罄 + 「该挂单额度已售罄」 | 前往查看其他挂单 |
| 已封盘 | 已封盘 + 「庄家已暂停该盘口收单」 | 前往查看其他挂单 |
| 已关闭 | 已关闭 + 「庄家已停止收单，挂单下架」 | 前往查看其他挂单 |
| 已结算 | 已结算 + 「赛事已结算，该挂单已完成」 | 前往查看其他挂单 |
| 赛事已开赛 | 赛事已开赛 + 「赛事已开赛，该挂单停止收单」 | 前往查看其他挂单 |
| 订单不存在（兜底） | 「该挂单已不存在」 | 前往查看其他挂单 |

### 3.5 分享元信息（Telegram 预览）

TG 预览由平台自动抓取，内容页需输出规范 OG 元信息，否则会话中只展示纯链接：

| 元信息 | 内容 |
|---|---|
| `og:title` | 赛事名称 + 投注项 |
| `og:description` | 玩法 + 卖出赔率 + 挂单状态 |
| `og:image` | 体育品牌图（不含动态数据，避免抓取缓存导致信息过期） |
| `og:url` | 分享链接自身 |

---

## 四、被邀请方回流（好友点开链接）

### 4.1 唤起体育：URL 参数

金刚点击 webview 后唤起体育 App，透传订单 ID。体育侧解析参数的优先级与既有规则一致（优先 hash 的 `?` 后段，退回 search）：

| 参数 | 必填 | 说明 |
|---|---|---|
| `orderId` | 是 | 庄单 ID，落地校验与定位的唯一依据 |
| `m` | 是 | 商户标识，决定回到哪个平台的体育入口 |
| `from` | 否 | 来源标记，建议固定 `share`，用于埋点 |

> 体育**不信任链接中携带的状态参数**，一律以落地时的服务端实时校验为准。

### 4.2 落地分流

**落地入口按商户归属路由**：接收方点击后进入的是链接中 `m` 所对应平台的体育入口，而非固定跳金刚。例如现金盘用户分享的链接，接收方点击后回到该现金盘的体育入口；`m` 缺失或非法时回落到金刚体育默认入口。

```
唤起体育 → 解析 orderId 与 m → 按 m 打开对应平台的体育入口 → 校验挂单状态
    ├── 有效（卖出中，含部分成交；有可卖额度；赛事未开赛）
    │     → 进入该挂单所属赛事的比赛详情
    │     → 自动选中该投注项（赔率格选中态）
    │     → 自动弹出购物车弹层，带入该投注项及其卖单簿
    │     → 提示「已定位该 P2P 挂单，请输入投注金额」
    └── 失效
          → 进入赛事页（赛事列表），不打开购物车、不选中任何投注项
          → toast 提示「当前订单已失效，请选择其他p2p订单进行购买」
          → 不弹出需要用户操作的弹窗
```

- 有效落地后，买入、依序吃单、注单确认等流程完全沿用既有规则，不做差异处理；
- 允许接收方买入分享者本人的挂单（与既有卖单簿规则一致，本期不限制）；
- 「查看其他 P2P 挂单」= 关闭弹窗并展示存在 P2P 挂单的赛事列表。

### 4.3 未登录处理

- 接收方未登录时，按体育既有登录流程引导登录；
- 登录成功后**继续完成落地**（定位投注项 + 弹出购物车），不要求用户重新点击链接；
- 取消登录则停留在赛事列表；
- 用户身份由宿主 App 通过 token 透传，体育侧不做注册，无下载引导页与注册页；
- 落地完成后接收方与普通用户完全一致，不做差异化路由。

---

## 五、相关数据字段与开关

分享**没有专用 socket 协议**，只涉及一个公开只读查询与两个配置项。

### 5.1 配置项

| 配置 | 位置 | 默认 | 说明 |
|---|---|---|---|
| `p2p_share_enabled` | 全局配置（新增） | `0` | 分享功能开关，用于灰度；关闭时分享入口隐藏，已生成链接展示「功能暂不可用」 |
| `p2p_share_host` | 全局配置（新增） | — | 分享链接域名，便于环境切换与域名更换 |
| `p2p_enabled` / 商户 P2P 开关 | 既有 | — | P2P 总开关关闭时，分享入口隐藏 |

### 5.2 内容页数据来源

内容页只读取挂单的公开字段，返回内容需满足 3.2 与 3.3：

| 字段 | 用途 |
|---|---|
| 庄单 ID、庄家标识 | 页面展示与落地定位 |
| 赛事 ID、联赛、主客队、开赛时间 | 赛事区展示与开赛判定 |
| 玩法、投注项、投注项 key | 挂单区展示与落地选中 |
| 卖出赔率（港盘） | 挂单区展示 |
| 挂单状态、剩余可卖额度（**仅用于服务端判定，不下发展示**） | 状态判定与主按钮切换 |

### 5.3 失效判定（5 种 + 1 兜底）

已售罄 / 已封盘 / 已关闭 / 已结算 / 赛事已开赛；数据不存在时按兜底态「该挂单已不存在」处理，落地流程与失效一致。

---

## 六、待金刚 / IM 侧确认事项

| # | 事项 | 说明 |
|---|---|---|
| 1 | **webview 消息体的分享类型** | 体育需要「按链接实时渲染内容」的 webview 卡片，请提供对应的 `type` 与 `show_type` 取值及参数约定 |
| 2 | **实时拉取策略** | 需确认金刚每次渲染 / 刷新消息体都会重新请求链接内容，不使用本地缓存快照；否则接收方会看到过期赔率与状态 |
| 3 | **唤起参数命名与格式** | 体育侧按 `orderId` 解析，若金刚有既有命名约定（如 `game_params` 拼装）请提供，体育侧适配 |
| 4 | **未安装体育 App 的处理** | 按金刚既有规则引导，体育侧不做处理，请确认现有引导链路可用 |
| 5 | **分享标题多语言** | `subGameName` 需要哪些语种（`ch/en/zh/th/vi`），体育按需下发 |
| 6 | **消息体点击区域** | 确认整卡点击唤起，还是仅按钮区域可点 |
| 7 | **各商户体育入口地址** | 按 `m` 路由落地需要各商户体育入口的可唤起地址，由商户接入时提供并维护映射表 |
| 8 | **TG 分享标题与描述** | `t.me/share/url` 的 `text` 参数内容，建议为「赛事 + 投注项 + 赔率」摘要，需确认多语言处理 |
